Consumes more resources, High GB memory and takes
more time to boot and more isolations.
Docker Containers - Hardware, OS, Docker helps to allow
multiple containers which has package of
applications, libs, dependencies, no guest OS share the